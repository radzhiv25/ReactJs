export const About = (props) => {
    return (
        <div className="">
            <p>Hey 👋, This is {props.name} and I am a passionate {props.job}. Age: {props.age}. </p>
        </div>
    )
}

// export default About